resources:
 containers:
   - container: mycontainer
     connection: "My Docker Connection"
     image: cwiederspan/bastion

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'